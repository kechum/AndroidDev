ext{


    appcompatVersion = '25.3.1'

    appcompatDependencies = [
            appcompat   :   "com.android.support:appcompat-v7:${appcompatVersion}",
            design      :   "com.android.support:design:${appcompatVersion}"
    ]


    glideVersion = '3.7.0'

    glideDependencies = [  glide    :   "com.github.bumptech.glide:glide:${glideVersion}"]


    googleVersion = '2.6.2'

    googleDependecies = [
            gson    :   "com.google.code.gson:gson:${googleVersion}"
    ]

    retrofitVersion = '2.1.0'


    retrofitDependencies = [
            retrofit    :   "com.squareup.retrofit2:retrofit:${retrofitVersion}",
            adapter    :   "com.squareup.retrofit2:adapter-rxjava:${retrofitVersion}",
            converter    :   "com.squareup.retrofit2:converter-gson:${retrofitVersion}",
            scalars    :   "com.squareup.retrofit2:converter-scalars:${retrofitVersion}"

    ]

    squareupVersion = '3.8.0'

            squareupDependencies = [
                    interceptor: "com.squareup.okhttp3:logging-interceptor:${squareupVersion}"
            ]

    rxandroidVersion = '1.2.1'

    rxandroidDependencies = [
            rxandroid   :   "io.reactivex:rxandroid:${rxandroidVersion}",
    ]


    rxjavaVersion = '1.1.6'

    rxjavaDependencies = [
            rxJava   :   "io.reactivex:rxjava:${rxjavaVersion}",
    ]


    butterknifeVersion = '8.2.1'


    butterknifeDependencies = [
            butterknife :   "com.jakewharton:butterknife:${butterknifeVersion}"
    ]


    recyclerVersion = '24.2.0'

    recyclerDependencies = [
            cardview :   "com.android.support:cardview-v7:${recyclerVersion}",
            gridlayout :   "com.android.support:gridlayout-v7:${recyclerVersion}",
            recycler :   "com.android.support:recyclerview-v7:${recyclerVersion}"
    ]

    daggerVersion = '2.9'
    daggerDependencies = [
            dagger   : "com.google.dagger:dagger:${daggerVersion}"
    ]

    gmsVersion = '3.1.0'

    gmsDependencies = [
            gms : "com.google.gms:google-services:${gmsVersion}"
    ]

    othersDependencies = [
            crashlytics :"com.crashlytics.sdk.android:crashlytics:2.6.8@aar"
    ]



}